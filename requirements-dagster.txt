# Phase 3C control-plane runtime. Versions intentionally pinned as a tested baseline target.
dagster==1.13.18
dagster-webserver==1.13.18
dagster-dbt==0.29.18

# Dagster host compatibility runtime. dagster-dbt 0.29.18 requires dbt-core >=1.7,<1.12.
# The canonical dbt 1.12.2 modeling runtime stays isolated in requirements-dbt.txt.
dbt-core==1.11.14
dbt-spark[PyHive]==1.11.0
PyYAML==6.0.3

# Real Shopify Admin GraphQL extraction executed by the Dagster host process.
requests>=2.32,<3.0
